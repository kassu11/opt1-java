# Info

The application uses `Jakarta EE` and `Hibernate` to **row localize** the user data inside local **SQL database**. The application supports three languages (English, Persion and Japanese). The user interface is rendered using `JavaFx`
